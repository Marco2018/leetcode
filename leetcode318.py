'''class Solution:    def maxProduct(self, input):        """        :type words: List[str]        :rtype: int        """        ans=0        for i in range(len(input)):            for j in range(len(input)):                if set(input[i]).difference(input[j])==set(input[i]) and set(input[j]).difference(input[i])==set(input[j]):                    ans=max(ans,len(input[i])*len(input[j]))        return ans'''class Solution:    def maxProduct(self, input):        """        :type words: List[str]        :rtype: int        """        d={}        for w in inputs:            d[w]=set(w)#这里把set操作做了 不然后面更加费时间        ans=0        for i in range(len(input)):            for j in range(len(input)):                if not d[input[i]] & d[input[j]]:                    ans=max(ans,len(input[i])*len(input[j]))        return anssolution=Solution()input=["a","ab","abc","d","cd","bcd","abcd"]print(solution.maxProduct(input))