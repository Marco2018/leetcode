class Solution:    def findRepeatedDnaSequences(self, s):        """        :type s: str        :rtype: List[str]        """        n=len(s)        if n<=10:            return []        nums,ans={},[]        for i in range(9,n,1):            temp=s[i-9:i+1]            if temp not in nums:                nums[temp]=1            elif temp not in ans:                ans.append(temp)        return ans使用dict hash速度更快，如果用nums=[]则TLE